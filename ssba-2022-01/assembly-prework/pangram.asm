section .text
    global pangram

pangram:
    ; initialize 26 bits to zero (mapping index to char - 'a')
    ; parse through string
        ; flip bits to 1 if char
    ; return if bits all equal to 1
	ret
